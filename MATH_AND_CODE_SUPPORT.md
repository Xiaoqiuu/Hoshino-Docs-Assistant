# 数学公式和代码高亮支持 📐💻

## ✨ 新增功能

### 1. KaTeX 数学公式支持

#### 支持的公式类型

**行内公式**
```markdown
这是行内公式 $f(x) = x^2$ 的示例
```
显示效果：这是行内公式 f(x) = x² 的示例

**块级公式**
```markdown
$$
\int_{a}^{b} f(x)dx = F(b) - F(a)
$$
```
显示效果：居中显示的大公式

#### 示例公式

**罗尔中值定理**
```markdown
应用罗尔中值定理：由于 $g(x)$ 满足罗尔定理条件，存在 $\xi \in (a,b)$ 使得

$$
g'(\xi) = 0
$$

计算导数：

$$
g'(x) = f'(x) - \frac{f(b)-f(a)}{b-a}
$$

代入 $x = \xi$：

$$
f'(\xi) - \frac{f(b)-f(a)}{b-a} = 0
$$
```

**常用数学符号**
- 分数：`$\frac{a}{b}$` → a/b
- 根号：`$\sqrt{x}$` → √x
- 求和：`$\sum_{i=1}^{n}$` → Σ
- 积分：`$\int_{a}^{b}$` → ∫
- 极限：`$\lim_{x \to \infty}$` → lim
- 希腊字母：`$\alpha, \beta, \gamma$` → α, β, γ

### 2. 代码语法高亮

#### 支持的语言
- JavaScript / TypeScript
- Python
- Java / C / C++
- HTML / CSS
- SQL
- Bash / Shell
- JSON / YAML
- Markdown
- 等 100+ 种语言

#### 代码块功能

**语法**
````markdown
```javascript
function hello() {
  console.log("Hello, World!");
}
```
````

**功能特点**
1. ✅ 语法高亮（VS Code Dark+ 主题）
2. ✅ 语言标签显示
3. ✅ 一键复制按钮
4. ✅ 深色主题代码块
5. ✅ 行号显示（可选）

#### 代码块界面

```
┌─────────────────────────────────┐
│ javascript          [复制]      │ ← 头部
├─────────────────────────────────┤
│ function hello() {              │
│   console.log("Hello!");        │ ← 高亮代码
│ }                               │
└─────────────────────────────────┘
```

### 3. 一键复制功能

**使用方法**
1. 鼠标悬停在代码块上
2. 点击右上角"复制"按钮
3. 代码自动复制到剪贴板
4. 按钮显示"✓ 已复制"（2秒后恢复）

**复制按钮样式**
- 粉红色主题
- 悬停高亮
- 点击反馈

## 🎨 样式设计

### 行内代码
```css
- 背景: 粉红色半透明
- 颜色: #f5abb9
- 圆角: 4px
- 字体: Consolas, Monaco
```

### 代码块
```css
- 背景: #1e1e1e (深色)
- 主题: VS Code Dark+
- 圆角: 8px
- 阴影: 0 2px 8px rgba(0, 0, 0, 0.15)
```

### 数学公式
```css
- 字体大小: 1.1em
- 块级公式: 居中显示，上下边距 16px
- 行内公式: 与文字对齐
- 用户消息: 白色公式
- AI 回复: 深色公式
```

## 📦 依赖包

### 已安装
```json
{
  "katex": "^0.16.25",
  "react-syntax-highlighter": "^16.1.0",
  "rehype-katex": "^7.0.1",
  "remark-gfm": "^4.0.1",
  "remark-math": "^6.0.0",
  "@types/react-syntax-highlighter": "^15.5.13"
}
```

### 功能说明
- **katex**: LaTeX 数学公式渲染
- **react-syntax-highlighter**: 代码语法高亮
- **rehype-katex**: Markdown 中的 KaTeX 支持
- **remark-math**: 解析数学公式语法
- **remark-gfm**: GitHub Flavored Markdown 支持

## 🔧 技术实现

### ReactMarkdown 配置
```typescript
<ReactMarkdown
  remarkPlugins={[remarkMath, remarkGfm]}
  rehypePlugins={[rehypeKatex]}
  components={{
    code({ node, inline, className, children, ...props }) {
      // 自定义代码块渲染
      const match = /language-(\w+)/.exec(className || '');
      const codeString = String(children).replace(/\n$/, '');
      
      return !inline && match ? (
        // 代码块：使用 SyntaxHighlighter
        <SyntaxHighlighter
          style={vscDarkPlus}
          language={match[1]}
        >
          {codeString}
        </SyntaxHighlighter>
      ) : (
        // 行内代码：普通 code 标签
        <code className={className}>{children}</code>
      );
    },
  }}
>
  {msg.content}
</ReactMarkdown>
```

### 复制功能实现
```typescript
onClick={() => {
  navigator.clipboard.writeText(codeString);
  const btn = event?.currentTarget as HTMLButtonElement;
  const originalText = btn.textContent;
  btn.textContent = '✓ 已复制';
  setTimeout(() => {
    btn.textContent = originalText || '复制';
  }, 2000);
}}
```

## 📝 使用示例

### 示例 1: 数学问题
**用户提问：**
```
解释罗尔中值定理
```

**AI 回复：**
```markdown
罗尔中值定理：设函数 $f(x)$ 在闭区间 $[a,b]$ 上连续，在开区间 $(a,b)$ 内可导，
且 $f(a) = f(b)$，则至少存在一点 $\xi \in (a,b)$，使得：

$$
f'(\xi) = 0
$$

这意味着在区间内至少有一点的切线是水平的。
```

### 示例 2: 代码问题
**用户提问：**
```
写一个 Python 快速排序
```

**AI 回复：**
````markdown
这是 Python 快速排序的实现：

```python
def quicksort(arr):
    if len(arr) <= 1:
        return arr
    
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    
    return quicksort(left) + middle + quicksort(right)

# 使用示例
numbers = [3, 6, 8, 10, 1, 2, 1]
sorted_numbers = quicksort(numbers)
print(sorted_numbers)
```

时间复杂度：平均 $O(n \log n)$，最坏 $O(n^2)$
````

### 示例 3: 混合内容
**用户提问：**
```
用 JavaScript 实现二次方程求解
```

**AI 回复：**
````markdown
二次方程 $ax^2 + bx + c = 0$ 的求解公式：

$$
x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
$$

JavaScript 实现：

```javascript
function solveQuadratic(a, b, c) {
  const discriminant = b * b - 4 * a * c;
  
  if (discriminant < 0) {
    return "无实数解";
  }
  
  const x1 = (-b + Math.sqrt(discriminant)) / (2 * a);
  const x2 = (-b - Math.sqrt(discriminant)) / (2 * a);
  
  return { x1, x2 };
}

// 示例：求解 x² - 5x + 6 = 0
console.log(solveQuadratic(1, -5, 6));
// 输出: { x1: 3, x2: 2 }
```

判别式 $\Delta = b^2 - 4ac$ 决定解的类型：
- $\Delta > 0$：两个不同实根
- $\Delta = 0$：一个重根
- $\Delta < 0$：无实根
````

## 🎯 支持的 Markdown 扩展

### GitHub Flavored Markdown (GFM)
- ✅ 表格
- ✅ 任务列表
- ✅ 删除线
- ✅ 自动链接

### 数学公式
- ✅ 行内公式 `$...$`
- ✅ 块级公式 `$$...$$`
- ✅ LaTeX 语法

### 代码块
- ✅ 语法高亮
- ✅ 语言标识
- ✅ 一键复制

## 🎨 视觉效果

### 代码块主题
使用 VS Code Dark+ 主题：
- 关键字：粉红色/紫色
- 字符串：橙色
- 注释：绿色
- 函数：黄色
- 数字：浅绿色

### 复制按钮
```css
- 默认: 粉红色半透明背景
- 悬停: 粉红色边框高亮
- 点击: 缩放动画
- 成功: 显示 "✓ 已复制"
```

## 📊 功能对比

| 功能 | 之前 | 现在 |
|------|------|------|
| 数学公式 | ❌ 纯文本 | ✅ KaTeX 渲染 |
| 代码高亮 | ❌ 无高亮 | ✅ 语法高亮 |
| 代码复制 | ❌ 手动选择 | ✅ 一键复制 |
| 代码主题 | ❌ 无 | ✅ VS Code Dark+ |
| 语言标签 | ❌ 无 | ✅ 显示语言 |

## 🚀 使用建议

### 数学公式
1. 使用 `$` 包裹行内公式
2. 使用 `$$` 包裹块级公式
3. 支持完整的 LaTeX 语法

### 代码块
1. 使用三个反引号 ``` 开始代码块
2. 指定语言以启用高亮
3. 点击复制按钮快速复制代码

### 最佳实践
- 复杂公式使用块级显示
- 代码块始终指定语言
- 长代码分段展示

## ✨ 完成！

现在 Hoshino 支持：
- ✅ LaTeX 数学公式渲染
- ✅ 100+ 种语言代码高亮
- ✅ 一键复制代码
- ✅ VS Code 主题
- ✅ 粉红色 UI 主题

可以处理数学、编程等各类专业问题了！
